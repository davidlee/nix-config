# Generated by scripts/named-workspace. Hand edits are fine: one
# [[workspace]] entry per name. Included from config.toml as an optional file,
# so deleting it just removes every workspace declared here.
