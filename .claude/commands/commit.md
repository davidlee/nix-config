Ensure code is formatted, correct, and tests pass.
`just check` is usually sufficient.

If the project has a linter, ensure the rules for linting are observed.

Ensure any relevant documentation is up to date.

Then:
- add all modified and new files to git. 
- if you think some files should be excluded, or committed in separate cohesive commits, present options to the user. 
- commit with a clear and concise one-line commit message, using semantic commit notation.
